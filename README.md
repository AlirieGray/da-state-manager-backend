## Development

To run locally:

1. Clone this repository

1. Set up a .env file in the root directory with your MongoDB username, password, and JWT public & private keys

1. Run the command `npm install` to install dependencies

1. Run the command `npm start`

## Credit

The user authorization and session handling code is from [TomDoesTech's REST API tutorial](https://github.com/TomDoesTech/REST-API-Tutorial-Updated)